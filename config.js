/**
 * CS Thinking Library — Configuration
 * Change API_BASE_URL to your deployed backend URL in production.
 */
const CONFIG = {
    API_BASE_URL: 'https://red-code-dump-backend.vercel.app',
    PROBLEMS_PER_PAGE: 10,
};
